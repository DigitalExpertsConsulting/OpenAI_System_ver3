App As appinfo:
    BackEnabled: =false
    OnStart: |
        =Refresh(똑실이_GNB_MENU);
        Concurrent(
        
        Set(Choi, PlainText(Last(똑실이_PowerBiPortalSample).imgtest));,
        Set(gnbColor1, Last(똑실이_PowerBiPortalSample).GNBbgColor);,           // 313B4A
        Set(LV1Selected1, Last(똑실이_PowerBiPortalSample).LV1SelectedColor);,  // 212A37
        Set(LV2BGColor1, Last(똑실이_PowerBiPortalSample).LV2bgColor);,        // 262E3A
        Set(LV2Selected1, Last(똑실이_PowerBiPortalSample).LV2SelectedColor);,  // 111B2A
        Set(Signature1, Last(똑실이_PowerBiPortalSample).SignatureColor);,     // F37321
        Set(FontColor1, Last(똑실이_PowerBiPortalSample).FontColor);,          // FFFFFF
        Set(gnbFont1, Last(똑실이_PowerBiPortalSample).Font);,                 // 'Open Sans'
        Set(FontSize1, Last(똑실이_PowerBiPortalSample).FontSize);,
        Set(Logo2, Last(똑실이_PowerBiPortalSample.imgtest2));,
        
        ForAll(
            똑실이_AzureGroup,
            Collect(
                col_ADGroup,
                AzureAD.GetGroup(Group_ID)
            )
        );
        
        // NewBiPortal
        ClearCollect(
            NewPortalCollect,
            ShowColumns(
                SortByColumns(똑실이_GNB_MENU, "LV1Order", SortOrder.Ascending),
                "Title",
                "OData__x004c_V1",
                "LV1Order",
                "LV2Order",
                "ReportURI",
                "ID"
            )
        );,
        
        ClearCollect(NewMACollect, ShowColumns(똑실이_GNB_MENU,"Title","OData__x004c_V1","LV1Order","LV2Order","ID","Teams","Users"));
        
        Clear(LikeTable);,
        Refresh(똑실이_QA_table);
        
        
        );
        
        
        ForAll(
            Sequence(CountRows(Filter(똑실이_GNB_MENU, IsNumeric(LV1) || IsBlank(LV1)))),
            Remove(똑실이_GNB_MENU, First(Filter(똑실이_GNB_MENU, IsNumeric(LV1) || IsBlank(LV1))))
        );
        ForAll(
            Sequence(CountRows(Filter(똑실이_GNB_MENU, IsNumeric(제목) || IsBlank(제목)))),
            Remove(똑실이_GNB_MENU, First(Filter(똑실이_GNB_MENU, IsNumeric(제목) || IsBlank(제목))))
        );
        
           Sequence(CountRows(Filter(똑실이_GNB_MENU, IsNumeric(LV1) || IsBlank(LV1))));
            Sequence(CountRows(Filter(똑실이_GNB_MENU, IsNumeric(제목) || IsBlank(제목))));
        
        //test
        //Set(Media, AddMediaButton1.Media);
        
        //카테고리 액세스 권환 컬렉션
        ClearCollect(
            Category_Auth, 
            { 
                LV1_Category: "", 
                LV2_Category: "", 
                Email: "" 
            }
        );
        Clear(Category_Auth);
    StartScreen: =QALog

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

