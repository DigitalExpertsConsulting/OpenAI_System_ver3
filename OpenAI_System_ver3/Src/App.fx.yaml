App As appinfo:
    BackEnabled: =false
    OnStart: |+
        =Set(LodingPopup,true);
        Refresh(똑실이_GNB_MENU);
        Concurrent(
        
        //Set(Choi, PlainText(Last(GNB_CUSTOM).imgtest));,
        Set(gnbColor1, Last(똑실이_GNB_CUSTOM).GNBbgColor);,           // 313B4A
        Set(LV1Selected1, Last(똑실이_GNB_CUSTOM).LV1SelectedColor);,  // 212A37
        Set(LV2BGColor1, Last(똑실이_GNB_CUSTOM).LV2bgColor);,        // 262E3A
        Set(LV2Selected1, Last(똑실이_GNB_CUSTOM).LV2SelectedColor);,  // 111B2A
        Set(Signature1, Last(똑실이_GNB_CUSTOM).SignatureColor);,     // F37321
        Set(FontColor1, Last(똑실이_GNB_CUSTOM).FontColor);,          // FFFFFF
        Set(gnbFont1, Last(똑실이_GNB_CUSTOM).Font);,                 // 'Open Sans'
        Set(FontSize1, Last(똑실이_GNB_CUSTOM).FontSize);,
        Set(Logo, Last(똑실이_GNB_CUSTOM.LogoImage));,
        
        ForAll(
            똑실이_AzureGroup,
            Collect(
                col_ADGroup,
                AzureAD.GetGroup(Group_ID)
            )
        );
        
        // NewBiPortal
        ClearCollect(
            NewPortalCollect,
            ShowColumns(
                SortByColumns(똑실이_GNB_MENU, "LV1Order", SortOrder.Ascending),
                "Title",
                "Menu_LV1",
                "LV1Order",
                "LV2Order",
                "ReportURI",
                "ID"
            )
        );,
        
        ClearCollect(NewMACollect, ShowColumns(똑실이_GNB_MENU,"Title","Menu_LV1","LV1Order","LV2Order","ID","Teams","Users"));
        
        Clear(LikeTable);,
        Refresh(똑실이_QA_table);
        
        
        );
        
        
        ForAll(
            Sequence(CountRows(Filter(똑실이_GNB_MENU, IsNumeric(Menu_LV1) || IsBlank(Menu_LV1)))),
            Remove(똑실이_GNB_MENU, First(Filter(똑실이_GNB_MENU, IsNumeric(Menu_LV1) || IsBlank(Menu_LV1))))
        );
        ForAll(
            Sequence(CountRows(Filter(똑실이_GNB_MENU, IsNumeric(Title) || IsBlank(Title)))),
            Remove(똑실이_GNB_MENU, First(Filter(똑실이_GNB_MENU, IsNumeric(Title) || IsBlank(Title))))
        );
        
           Sequence(CountRows(Filter(똑실이_GNB_MENU, IsNumeric(Menu_LV1) || IsBlank(Menu_LV1))));
            Sequence(CountRows(Filter(똑실이_GNB_MENU, IsNumeric(Title) || IsBlank(Title))));
        
        //test
        //Set(Media, AddMediaButton1.Media);
        
        //카테고리 액세스 권환 컬렉션
        ClearCollect(
            Category_Auth, 
            { 
                LV1_Category: "", 
                LV2_Category: "", 
                Email: "" 
            }
        );
        Clear(Category_Auth);
        Set(LodingPopup,false);
        
        Clear(gnbdist);
        Clear(gnbgru);
        ForAll(
            똑실이_GNB_MENU,
            Collect(
                gnbdist,
                ThisRecord.Teams
            )
        );
        ForAll(
            gnbdist,
            If(
                CountRows(
                    Filter(
                        Office365그룹.ListGroupMembers(
                            Mid(
                                ThisRecord.Claims,
                                40
                            )
                        ).value,
                        User().Email in mail
                    )
                ) > 0,
                Collect(
                    gnbgru,
                    {Email: ThisRecord.Email}
                )
            )
        );
        Distinct(
            gnbgru,
            ThisRecord
        )
        
        
    StartScreen: =QALog

    Host As hostControl.DefaultHostControlVariant:
        OnCancel: =false
        OnEdit: =false
        OnNew: =false
        OnSave: =false
        OnView: =false

